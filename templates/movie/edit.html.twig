
{% include 'default/_form.html.twig' with {
    page_title: 'title_movie_edit',
    page_title_param: {'%id%': movie.id},
    attr: {
        method: 'PUT',
        action: url('movie_edit', {'id': movie.id}),
    },
    back_to_list_path: 'movie_index',
} %}